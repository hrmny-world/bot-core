import eval from './admin/maintenance/eval';
import repeat from './admin/maintenance/repeat';
import botInvite from './info/bot-invite';
import help from './info/help';
import info from './info/info';
import ping from './info/ping';

export { eval, repeat, botInvite, help, info, ping };

export default [eval, repeat, botInvite, help, info, ping];
